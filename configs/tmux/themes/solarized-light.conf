# ── Solarized Light — tmux color tokens ──────────────────────────────────────
# Palette reference (Ethan Schoonover): same accents, light backgrounds
#   base3=#fdf6e3  base2=#eee8d5  base1=#93a1a1  base0=#839496
#   base00=#657b83 base01=#586e75 base02=#073642 base03=#002b36

set -g @bg       "#fdf6e3"
set -g @surface  "#eee8d5"
set -g @text     "#657b83"
set -g @subtle   "#586e75"
set -g @muted    "#93a1a1"
set -g @accent   "#268bd2"
set -g @accent2  "#cb4b16"
set -g @ok       "#859900"
set -g @warn     "#b58900"
set -g @err      "#dc322f"
set -g @select   "#6c71c4"
